<div class="flex flex-col">
  <h1 class="font-bold text-2xl mb-4">관리자 - 채팅방 관리</h1>

  <table class="min-w-full bg-white border">
    <thead>
      <tr>
        <th class="py-2 px-4 border-b">제목</th>
        <th class="py-2 px-4 border-b">설명</th>
        <th class="py-2 px-4 border-b">활성화</th>
        <th class="py-2 px-4 border-b">액션</th>
      </tr>
    </thead>
    <tbody>
      <% @chat_rooms.each do |chat_room| %>
        <tr>
          <td class="py-2 px-4 border-b"><%= chat_room.title %></td>
          <td class="py-2 px-4 border-b"><%= chat_room.description %></td>
          <td class="py-2 px-4 border-b"><%= chat_room.active ? "예" : "아니오" %></td>
          <td class="py-2 px-4 border-b">
            <%= link_to "수정", edit_admin_chat_room_path(chat_room), class: "text-blue-500 hover:underline mr-2" %>
            <%= link_to "삭제", admin_chat_room_path(chat_room), data: { turbo_method: :delete, turbo_confirm: "정말 삭제하시겠습니까?" }, class: "text-red-500 hover:underline" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= link_to "새 채팅방 생성", new_admin_chat_room_path, class: "mt-6 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded-lg self-start" %>
</div>