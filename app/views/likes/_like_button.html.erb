<%# Vote System (v3.5) - Consolidated Like/Dislike Buttons %>
<div id="vote-buttons-<%= post.id %>" class="flex gap-2 sm:gap-3">
  <%# Like Button %>
  <button onclick="votePost(event, <%= post.id %>, 1)" 
          class="flex items-center gap-1.5 sm:gap-2 border border-neutral-700 text-gray-400 px-3 sm:px-4 py-1.5 sm:py-2 text-xs sm:text-sm hover:text-green-400 hover:border-green-500 hover:bg-neutral-800 transition active:scale-95 group">
    <span>👍</span>
    <span id="like-count-<%= post.id %>" class="group-hover:text-green-400"><%= post.like_count %></span>
    <span class="hidden xs:inline">추천</span>
  </button>


</div>
